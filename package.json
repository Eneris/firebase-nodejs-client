{
  "name": "firebase-node",
  "version": "0.1.0",
  "description": "Firebase NodeJS client",
  "author": "Martin Kalabek<martin.kalabek@gmail.com>",
  "license": "Apache-2.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package*",
    "*.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/eneris/push-receiver.git"
  },
  "keywords": [
    "push",
    "service",
    "fcm",
    "gcm",
    "notifications",
    "node",
    "electron",
    "receiver"
  ],
  "scripts": {
    "build": "yarn clean && yarn tsc",
    "compile": "yarn protos:build && yarn tsc --incremental",
    "prepublish": "yarn build",
    "lint": "yarn eslint src --ext .ts",
    "clean": "rm -rf dist",
    "precommit": "yarn lint && yarn build",
    "prepare": "yarn prepublish"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "axios-retry": "^3.3.1",
    "eventemitter3": "^4.0.7"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "@typescript-eslint/eslint-plugin": "^5.38.0",
    "@typescript-eslint/parser": "^5.38.1",
    "eslint": "^8.24.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "engines": {
    "node": ">=16"
  }
}